/* Arrays

let arr = new Array();
let arr = [];

*/

let fruits = ["Banana", "Apple", "Kiwi"];   //[0,1,2,] starts from zero
console.log(fruits.length);
//console.log(fruits[0]);     // Banana
// console.log(fruits[fruits.length - 1]); // Kiwi
// to get last element use ".at" console.log(fruits.at(-1)); // Kiwi
// number of elements console.log(fruits.length); // 3